node_modules/
dist/
*.local
__pycache__/
*.pyc

# Secrets — GCP service account keys etc.
*.json.key
tidy-nomad-*.json
.env
.env.*
# Not a secret — the public Cloud Run backend URL, needed to reproduce GitHub Pages builds
!frontend/.env.production
